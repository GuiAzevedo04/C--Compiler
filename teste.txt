int a, teste__b;
a = 42;
b = -7;
flag = !flag || (a > b && b <= 100);

if (a == 42) {
    print(a);
} else {
    while (b != 0) {
        b = b - 1;
    }
}

/* Comentários */
// comentário de linha única
/* comentário
   multilinha */

int erro1 = 12.5;   // número com ponto não permitido
int erro2 = 2abc;   // identificador inválido
int erro3 = 10 @ 2; // caractere inválido '@'
/* comentário não fechado...
